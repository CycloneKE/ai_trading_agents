[pytest]
# The unit/integration suite lives in tests/. The scripts/ directory holds
# manual dev/integration utilities (some named test_*.py) that are not part of
# the automated suite and may hit live APIs — keep pytest from collecting them.
testpaths = tests

markers =
    integration: marks tests that talk to live external services (deselect with '-m "not integration"').
