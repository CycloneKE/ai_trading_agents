# Minimal runtime dependencies for production backend
python-dotenv>=0.21.0
requests>=2.32.4
flask>=2.3.0
flask-cors>=4.0.0
prometheus-client>=0.12.0
redis>=4.5.3
psycopg2-binary>=2.9.0
cryptography>=3.4.8
websocket-client>=1.3.0
aiohttp>=3.8.0
psutil>=5.9.0
numpy>=1.22.0
pandas>=1.3.0

# Optional / extended features (install in separate build if needed)
# scikit-learn, tensorflow, torch, xgboost, optuna, scipy, matplotlib# Minimal runtime requirements (keep small to avoid heavy ML packages in the container)
python-dotenv>=0.19.0
requests>=2.32.4
flask>=2.3.0
flask-cors>=4.0.0
prometheus-client>=0.12.0
redis>=4.5.3
psycopg2-binary>=2.9.0

# Data and numeric packages required by the backend
numpy>=1.22.0
pandas>=1.3.0
psutil>=5.9.0

# Additional runtime packages required by the application
cryptography>=3.4.8
websocket-client>=1.3.0
aiohttp>=3.8.0
