# Core dependencies (updated for security)
numpy==2.5.3
pandas==2.3.3
scikit-learn==1.9.1
joblib==1.6.0
xgboost==3.4.1

# Data and API (updated for security)
requests==2.34.2  # Used for FMP, Alpha Vantage, and other API calls
redis==8.1.0
python-dotenv==1.2.3

# Database
psycopg2-binary==2.9.13
supabase==2.31.0

# Security
cryptography==46.0.3

# Real-time data
websocket-client==1.9.2

# Monitoring (updated for security)
prometheus-client==0.26.0
flask==3.1.3
flask-cors==6.0.5
gunicorn==26.2.0
waitress==3.0.2

# Machine Learning and RL
torch==2.14.0
optuna==4.9.0
psutil==7.2.2

# Logging and utilities
colorama==0.4.6

# Performance and Async
aiohttp==3.14.3
asyncio-throttle==1.0.2

# Enhanced Features
scipy==1.18.1
matplotlib==3.11.2

# Authentication
PyJWT==2.14.0
bcrypt==5.0.0

# PDF Ingest
pdfplumber>=0.11.0
pypdf>=4.0

# Technical indicators (optional). The only pandas_ta releases still on PyPI
# require Python >= 3.12, so it is version-gated here. src/agent/indicators.py
# provides a Wilder-equivalent RSI fallback when it is absent — without one,
# technical_strategy fails to import and the agent silently holds forever.
pandas_ta==0.4.71b0; python_version >= "3.12"

# Price feed used by src/utils/real_price_feed.py (was imported but undeclared)
yfinance>=0.2.60

# NLP / sentiment. All were imported by src/agent/sentiment_analyzer.py and
# src/agent/text_processor.py without appearing here, so a clean install could
# not import them — and because src/api/api_server.py imports the sentiment
# analyser at module scope, the REST API and dashboard never started.
# Each is now optional at runtime; install them to get real sentiment scores.
textblob>=0.18.0
vaderSentiment>=3.3.2
nltk>=3.9
anthropic>=1.8,<2  # Paid AI tier (src/agent/claude_provider.py); used only when ANTHROPIC_API_KEY is set
