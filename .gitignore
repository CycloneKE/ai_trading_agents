# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
.venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
logs/
*.log

# Data
data/
*.csv
*.json
!config.json
!package.json
!package-lock.json
!railway.json
!quick_deploy_config.json

# Secrets — NEVER commit these
secrets/*
!secrets/README.md
.env
.env.local
.env.production
*.pem
*.key

# Docker
# .dockerignore is NOT ignored. It decides what enters the image, so it
# has to be in version control: ignoring it meant every clone and every
# CI build ran `COPY . .` with no exclusions, copying .env, the SQLite
# journals and .git into the published image.

# Testing
.coverage
htmlcov/
.pytest_cache/

# Build
node_modules/
.npm
.eslintcache
.next/

# Misc
.DS_Store
Thumbs.db
scratch/
backups/
# Coverage artifacts
coverage.xml

# Timestamped config backups written by scripts/manage_universe.py --apply
config/*.bak.*
